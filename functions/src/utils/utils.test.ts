import { adder } from '.';

describe('adder', () => {
  test('should take any number of numbers', () => {});
  test('should sum any number of numbers', () => {});
  test('should sum numbers including negatives', () => {});
});
